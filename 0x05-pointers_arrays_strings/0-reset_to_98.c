#include <stdio.h>
#include "main.h"

/**
 * reset_to_98 - updates the value stored at *n
 * n: value to be updated
 *
 * Return: Always return 0 on success
 */
void reset_to_98(int *n)
{
	int *n;

	*n = 98;
	printf("%d\n", *n);

	return (0);
}
