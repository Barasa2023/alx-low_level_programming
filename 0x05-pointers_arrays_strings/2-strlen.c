#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 * _strlen - calculate the length of a string
 * @s: string to find the length
 *
 * return: 0
 */
int _strlen(char *s)
{
	size_t strlen(char *s);

	return (0);
}
